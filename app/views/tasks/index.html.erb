<header class="text-center">
    <h1>タスク一覧</h1>
    <p><%= @user.name %>さんのタスク数 : <%= @tasks.length %></p>
</header>

<ol>
    <% @tasks.each do |task| %>
        <li><%= link_to task.title, user_task_path(user_id: @user.id, id: task.id) %></li>
  <% end %>
</ol>
